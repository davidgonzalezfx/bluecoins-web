export type prayer = {
  content: string;
  author: string;
};

export type testimony = {
  content: string;
  author: string;
};

export type resource = {
  url: string;
  title: string;
  description: string;
  image: string;
  siteName: string;
};
